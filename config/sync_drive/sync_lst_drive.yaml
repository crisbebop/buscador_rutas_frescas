google_drive:
  folder_name: "GEE_Exports_LST"
  file_extensions:
    #- ".geojson"
    - ".tif"

auth:
  credentials_path: "secrets/google_oauth_credentials.json"
  token_path: "secrets/google_oauth_token.json"

local:
  target_dir: "data/reference/lst"

sync:
  overwrite: false
  log_level: "INFO"
